name: Makefile CI

on:
  push:
    branches: [ "12.1" ]
  pull_request:
    branches: [ "12.1" ]

jobs:
  build: 

    runs-on: ubuntu-latest


    steps: 
    - uses: actions/checkout@v3

    - name: configure
      run: |
    - name: Install dependencies
      run: make

    - name: Run check
      run: |

    - name: Run distcheck
      run: |
